# 更新日志

## v1.2.0

本版本修复端口识别、列表选择和分组持久化问题，并拆分主界面代码以方便后续维护。

### 修复与改进

- 筛选、排序或刷新后清除旧选择，避免操作到其他端口。
- PID 不可读取时仍正确识别 socket 占用，三平台均查询监听和已连接的 TCP/UDP socket。
- 扫描和进程详情在后台读取，通过 UI 线程更新界面；重复刷新请求合并处理。
- 分组支持多选，备注窗口、分组管理和筛选共用端口归属数据。
- 修复旧版配置迁移，保存失败时不覆盖内存数据；新安装使用系统用户配置目录，已有配置路径继续兼容。
- 修复 macOS 已连接 UDP 的本地端口解析、GPU 输出越界和 macOS 安装包版本号。
- main.go 从 715 行缩减为 34 行，拆分界面构建、筛选排序、后台刷新、分组、备注和进程操作模块。

### 验证

- 65 个测试通过，race 检测和 go vet 通过，测试覆盖率 36.6%。
- macOS 原生构建通过；Windows/Linux 无界面交叉编译通过。

### 使用说明

“未观测到占用”仅表示系统查询没有返回该端口，不能保证端口可绑定。查询结果仍受权限和采样时刻影响。

安装包由 GitHub Actions 构建：Linux x86_64（deb/rpm/AppImage）、macOS arm64（dmg）、Windows x86_64（exe）。
